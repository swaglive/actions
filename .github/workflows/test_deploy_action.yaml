name: Test deploy-action
on:
  push:
    paths:
      - "deploy-action/**"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: docker build deploy-action
