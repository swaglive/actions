name: Test swag-deploy-action
on:
  push:
    paths:
      - "swag-deploy-action/**"
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run tests
        run: docker build swag-deploy-action
